{
  "name": "libraflow",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && bun install",
    "build": "cd backend && bun run build",
    "start": "cd backend && bun run start"
  },
  "dependencies": {
    "prisma": "6.2.1"
  }
}
